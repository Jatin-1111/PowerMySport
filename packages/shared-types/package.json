{
  "name": "@powermysport/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical TypeScript types shared across server, admin, client and community — the fix for User/Tournament/Booking having drifted into three independent definitions.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit"
  }
}
